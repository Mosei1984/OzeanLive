#include "particles.h"
#include "generated/particles.h"

// Re-export the generated particle sprites for linking
// (The generated file defines them, this makes them available to other compilation units)
